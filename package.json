{
  "name": "api-regressor99",
  "version": "0.1.0",
  "private": true,
  "description": "Continuous API performance regression detection platform.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web,mock -c blue,green,magenta \"npm run dev -w @regressor99/api\" \"npm run dev -w @regressor99/web\" \"npm run dev -w @regressor99/mock-target\"",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "format": "prettier --write ."
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.1",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  }
}
